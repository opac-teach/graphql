type Song {
  "The ID of the song"
  id: ID!
  "The name of the song"
  name: String!
  "user who created the song"
  user: User!
}

type Query {
  "Get all songs"
  songs: [Song!]!
  "Get a song by ID"
  songById(id: ID!): Song!
}
