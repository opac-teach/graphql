type Song {
  "The ID of the song"
  id: ID!
  "The name of the song"
  name: String!
  "The user of the song"
  user: User!
}

type Query {
  "Get all songs"
  songs: [Song!]!
  "GET a song by ID"
  song(id: ID!): Song!
}
